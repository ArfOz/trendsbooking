import React from 'react';
import { Box } from '@mui/material';
import { Typography } from '@mui/material';

function MainTop() {
    return <Box>
      <Typography>Tahir</Typography>
    </Box>;
}

export default MainTop;
