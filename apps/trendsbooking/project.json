{
  "name": "trendsbooking",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/trendsbooking/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/trendsbooking",
        "main": "apps/trendsbooking/src/main.ts",
        "tsConfig": "apps/trendsbooking/tsconfig.app.json",
        "assets": ["apps/trendsbooking/src/assets"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/trendsbooking/src/environments/environment.ts",
              "with": "apps/trendsbooking/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/js:node",
      "options": {
        "buildTarget": "trendsbooking:build"
      },
      "configurations": {
        "production": {
          "buildTarget": "trendsbooking:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/trendsbooking/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/trendsbooking/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
